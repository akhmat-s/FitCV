PyMuPDF==1.25.5
streamlit>=1.27.0
python-docx>=0.8.11
fastapi>=0.110.0
uvicorn>=0.29.0
pydantic>=2.6.0
openai>=1.30.0
python-multipart>=0.0.9
python-dotenv>=1.0.0
requests>=2.31.0

# test/dev only — async test runner for the async backend (no runtime impact; the
# AsyncOpenAI client ships with openai and uvicorn is already async).
pytest-asyncio>=0.23
